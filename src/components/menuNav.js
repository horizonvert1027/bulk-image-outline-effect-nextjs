"use client"
import React, { useEffect, useRef, useState } from "react"

export default function MenuNav({ hasCookie }) {

    return (
        <header className="fixed flex flex-col top-0 left-0 backdrop-blur-xl bg-transparent flex flex-wrap md:justify-start md:flex-nowrap z-30 w-full px-4 2xl:px-0">
            <nav className="h-10 relative max-w-screen-xl w-full mx-auto md:flex md:items-center md:justify-between md:gap-3 py-2">
                
            </nav>
        </header>
    )
}
